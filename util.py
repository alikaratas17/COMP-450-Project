def innerProd(l1,l2):
  if len(l1)==len(l2):
    s = 0
    for i in range(len(l1)):
      s+= l1[i]*l2[i]
    return s
def listScalarProd(l,s):
  for i in range(len(l1)):
    l1[i]=s*l1[i]
  return l1
